/*
 * import des objets de chaque fichier contenue dans le dossier courrant
 * on les déversent dans un objet qui va englober toutes les fonctions du dossier courrant
 * et on exporte ce dossier
 * exemple:
 * const auth = require('./auth');
 * const siteSocket = {
 *    ...auth,
 * };
 * module.exports = siteSocket;
 */
